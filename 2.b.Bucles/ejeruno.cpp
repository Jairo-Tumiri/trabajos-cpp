#include <iostream>
/* #include <String> */
using namespace std;

/* busqueda:
- secuencial (condicion y si cumple sale, recorre hasta encontrar)
- binaria (la divide en dos y luego va preguntando si cumple la condicion y si no pregunta si es mayor o menor cada vez
que no cu   mple elimina el bloque hasta encontrar)(solo funciona si esta ordenado) */
/* ordenar: ordeno con el array cargador */
/* insertarOrdenador: ordeno cuando inserto      */

// Funcion mostrar array
void mostrarArray(int array[], int size)
{
    for (int i = 0; i < size; i++)
    {
    }
};

int main()
{
    /*     int nuevaPalabra[11] = {12,23,4,2,12,3};
        int cantidad = sizeof(nuevaPalabra)/sizeof(nuevaPalabra[0]);
        cout << cantidad << endl; */
    /*     // getline char
        char nombre[30];
        cin.getline(nombre, sizeof(nombre));
        cout << nombre;

        // getline String
        string nombre2;
        cin >> nombre2;
        getline(cin,nombre2);
        cout << nombre2; */

    return 0;
}